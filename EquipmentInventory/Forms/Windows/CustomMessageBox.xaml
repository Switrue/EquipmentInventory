<Window x:Class="EquipmentInventory.Forms.Windows.CustomMessageBox"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EquipmentInventory.Forms.Windows"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Style="{StaticResource MaterialDesignWindow}"
        mc:Ignorable="d"
        Background="Transparent"
        AllowsTransparency="True"
        MinHeight="200" 
        Width="400"
        SizeToContent="Height" 
        ResizeMode="NoResize" 
        WindowStyle="None"
        WindowStartupLocation="CenterScreen"
        MouseDown="Window_MouseDown">

    <!--#region Styles-->
    
    <Window.Resources>
        <ResourceDictionary>
            <Style TargetType="Button" BasedOn="{StaticResource MaterialDesignRaisedSecondaryButton}">
                <Setter Property="IsEnabled" Value="{Binding DataContext.ControlsEnabled, RelativeSource={RelativeSource FindAncestor, AncestorType=Window}}" />
                <Setter Property="Width" Value="60" />
                <Setter Property="Margin" Value="5 0 5 0" />
            </Style>
        </ResourceDictionary>
    </Window.Resources>
    
    <!--#endregion-->
    
    <Border Style="{StaticResource WindowBorder}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- #region Header -->

            <Border Grid.Row="0" Style="{StaticResource TitleBarBorder}" MouseLeftButtonDown="DragWindow_MouseLeftButtonDown">
                <Grid Margin="2.5">
                    <TextBlock x:Name="customMessageBoxTitle" Style="{StaticResource WindowTitleTextBlock}" />
                    <WrapPanel HorizontalAlignment="Right">
                        <Button x:Name="closeMessageBoxBtn" Style="{StaticResource CloseButton}" Click="CloseWindow_Click" />
                    </WrapPanel>
                </Grid>
            </Border>

            <!-- #endregion -->

            <!-- #region Body -->

            <Grid Grid.Row="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="50" />
                </Grid.RowDefinitions>

                <TextBox x:Name="messageTxtB" 
                         Grid.Row="0" 
                         Margin="10"
                         TextWrapping="Wrap" 
                         TextAlignment="Justify"
                         VerticalAlignment="Center"
                         HorizontalAlignment="Center" 
                         IsReadOnly="True"
                         ContextMenu="{x:Null}"        
                         BorderBrush="Transparent"
                         materialDesign:TextFieldAssist.UnderlineBrush="Transparent"
                         Foreground="White" />

                <WrapPanel Grid.Row="2" VerticalAlignment="Bottom" HorizontalAlignment="Center" Margin="10">
                    <Button x:Name="trueBtn" Click="ReturnTrue_Click" />
                    <Button x:Name="falseBtn" Click="ReturnFalse_Click" Visibility="Collapsed" />
                </WrapPanel>
            </Grid>

            <!-- #endregion -->
        </Grid>
    </Border>
</Window> 
