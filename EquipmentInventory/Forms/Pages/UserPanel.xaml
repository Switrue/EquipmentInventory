<UserControl x:Class="EquipmentInventory.Forms.Pages.Admin.UserPanel"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:EquipmentInventory.Forms.Pages.Admin"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      mc:Ignorable="d"
      Background="Transparent"
      d:DesignHeight="200" d:DesignWidth="400">
    <!--#region Styles-->
    
    <UserControl.Resources>
        <ResourceDictionary>
            <Style TargetType="RadioButton" BasedOn="{StaticResource MaterialDesignTabRadioButton}">
                <Setter Property="Foreground" Value="{StaticResource TextColor}" />
                <Setter Property="materialDesign:RippleAssist.IsDisabled" Value="True" />
                <Setter Property="Padding" Value="5" />
                <Setter Property="IsTabStop" Value="False" />
                <Setter Property="FontSize" Value="18" />
                <Setter Property="Focusable" Value="False" />
            </Style>

            <Style TargetType="Button" BasedOn="{StaticResource MaterialDesignFlatSecondaryDarkButton}">
                <Setter Property="HorizontalContentAlignment" Value="Left" />
                <Setter Property="Foreground" Value="{StaticResource HintColor}" />
                <Setter Property="IsEnabled" Value="{Binding DataContext.ControlsEnabled, RelativeSource={RelativeSource FindAncestor, AncestorType=Window}}" />
            </Style>
        </ResourceDictionary>
    </UserControl.Resources>
    
    <!--#endregion-->
    
    <Grid VerticalAlignment="Center" HorizontalAlignment="Center">
        <!--#region Control panel-->
        
        <StackPanel x:Name="radioButtonContainer" Orientation="Horizontal">
            <RadioButton x:Name="userRegistrationRb" Click="RadioButton_Click" />
            <RadioButton x:Name="tablesRb" Click="RadioButton_Click" MouseEnter="DisplayTableOptions_Mouse" />
            <RadioButton x:Name="dictionariesRb" Click="RadioButton_Click" />

            <Popup x:Name="actionsPopupRb" 
                   PlacementTarget="{Binding ElementName=tablesRb}" 
                   AllowsTransparency="True"
                   Placement="Bottom" 
                   StaysOpen="False">
                <Border Padding="0 5 0 0">
                    <StackPanel Background="{StaticResource Background}" MouseLeave="DisplayTableOptions_Mouse">
                        <Button x:Name="archiveBtn" Click="Archive_Click" />
                        <Button x:Name="inventoryBtn" Click="Inventory_Click" />
                    </StackPanel>
                </Border>
            </Popup>
        </StackPanel>
        
        <!--#endregion-->
    </Grid>
</UserControl>
