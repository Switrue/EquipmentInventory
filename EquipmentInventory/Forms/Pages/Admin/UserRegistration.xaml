<Page x:Class="EquipmentInventory.Forms.Pages.UserRegistration"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:EquipmentInventory.Forms.Pages.Admin"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      mc:Ignorable="d"
      Background="Transparent"
      d:DesignHeight="450" d:DesignWidth="800"
      MouseDown="Page_MouseDown">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        
        <Border Grid.Column="1" Background="{StaticResource Background}" BorderThickness="2 0 2 0" BorderBrush="{StaticResource SecondaryColor}">
            <Grid Margin="40 10 40 10" Width="290" VerticalAlignment="Center">
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="40" />
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="50" />
                    <RowDefinition Height="auto" />
                </Grid.RowDefinitions>

                <WrapPanel Grid.Row="0">
                    <Image Source="/Resources/Pictures/SmallUserIcon.png" Width="80" Height="80" HorizontalAlignment="Left" />
                    <StackPanel Margin="10">
                        <Button Content="Выбрать изображение" FontSize="12" Padding="4" Height="25"
                                Style="{StaticResource MaterialDesignFlatButton}"
                                Foreground="{StaticResource HintColor}"/>
                    </StackPanel>
                </WrapPanel>

                <StackPanel Grid.Row="2">
                    <TextBox materialDesign:HintAssist.Hint="Имя" Margin="5"
                             Foreground="{StaticResource TextColor}"
                             BorderBrush="{StaticResource HintColor}" />
                    <TextBox materialDesign:HintAssist.Hint="Фамилия" Margin="5"
                             Foreground="{StaticResource TextColor}"
                             BorderBrush="{StaticResource HintColor}" />
                    <TextBox materialDesign:HintAssist.Hint="Логин" Margin="5"
                             Foreground="{StaticResource TextColor}"
                             BorderBrush="{StaticResource HintColor}" />
                    <TextBox materialDesign:HintAssist.Hint="Пароль" Margin="5"
                             Foreground="{StaticResource TextColor}"
                             BorderBrush="{StaticResource HintColor}" />
                </StackPanel>

                <Button Content="Зарегестрировать" Grid.Row="4"
                        materialDesign:RippleAssist.Feedback="#DD000000"
                        IsEnabled="{Binding DataContext.ControlsEnabled, RelativeSource={RelativeSource FindAncestor, AncestorType=Window}}"
                        Style="{StaticResource MaterialDesignRaisedSecondaryButton}" />
            </Grid>
        </Border>


        
    </Grid>
</Page>
