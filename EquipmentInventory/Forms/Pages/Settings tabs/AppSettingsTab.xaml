<UserControl x:Class="EquipmentInventory.Forms.Pages.Settings_tabs.AppSettingsTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:EquipmentInventory.Forms.Pages.Settings_tabs"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <!--#region Styles-->
    <UserControl.Resources>
        <ResourceDictionary Source="/Resources/Styles/UserStyles.xaml" />
    </UserControl.Resources>
    <!--#endregion-->

    <Grid>
        <!--#region Tab body-->
        <StackPanel>
            <TextBlock x:Name="titleTab" Style="{StaticResource SetupTabHeader}" />
            <ComboBox x:Name="appLanguageCB" 
                      ItemsSource="{Binding Languages}"
                      SelectedItem="{Binding SelectedLanguage}"
                      DisplayMemberPath="Name"
                      SelectedValuePath="Code" />
            <Button x:Name="applyCodeBtn" HorizontalAlignment="Right" Command="{Binding ApplyLanguageCommand}" />
        </StackPanel>
        <!--#endregion-->
    </Grid>
</UserControl>
